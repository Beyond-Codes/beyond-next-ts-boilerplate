import { IconType } from 'react-icons';

export type FooterSocialLink = {
  id: number;
  name: string;
  url: string;
  icon: IconType;
};

export type FooterLegalLink = {
  id: number;
  name: string;
  url: string;
};
